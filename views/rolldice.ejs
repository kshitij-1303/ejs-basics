<%- include("includes/head.ejs") %>
<body>
  <h1>Dice gave the value: <%= diceVal %>
  </h1>
  <% if(diceVal==6 || diceVal==3 || diceVal==1) { %>
    <h2>Nice roll!</h2>
    <% } %>
    <%- include("includes/footer.ejs") %>
</body>

</html>